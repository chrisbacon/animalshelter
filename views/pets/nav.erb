<nav>
	<ul>
		<li class="<%= @selected['all']%>">
			<a href="/pets">All</a>
		<li>
		<% for species in @all_species %>
		<li class="<%= @selected[species.id]%>">
			<a href="/pets/filter/<%= species.id %>"><%= species.name%></a>
		</li>
		<% end %>
	</ul>
</nav>
